import {Box, Typography} from '@mui/material';

interface ItemProp {
    fields: {ID: number; category: string; productName: string};
}

function Item() {
    return (
        <Box>
            <Typography>

            </Typography>
        </Box>
    )
}

export default Item